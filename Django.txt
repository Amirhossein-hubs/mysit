pip install -r requirements.txt
pip install Django>5.3,<5.3
django-admin startproject mysite 
django-admin startproject mysite .
python manage.py createsuperuser
python manage.py runserver

#fill project
manage.py:مدیریت دستورات کار با پروژه جنگو
Manage.py:این فایل ابزار خط فرمان پروژه است و ما از این فایل فقط برای استقرار، اشکال زدایی و آزمایش با پروژه استفاده خواهیم کرد.
debug==True:جنگو در صورت بروز خطا، یک صفحه خطای توسعه‌دهنده کامل شامل تمام جزئیات فنی (مانند متغیرهای محیطی، تنظیمات دیتابیس، و حتی بخش‌هایی از کد پایتون) را نمایش می‌دهد
debug==False:کاربر فقط یک صفحه خطای عمومی (معمولاً خطای 500) دریافت می‌کند و جزئیات فنی پنهان می‌مانند.
debug:باشد تا هرکس به خطای کامل و مسیر فایلش با خبرنباشهfalseدر هنگام بارگزاری سایت روی سرور باید 
__init__.py:پرونده خالی براساس استاندارد پایتون وجودارد
settings.py:پرونده اصلی است که در آن همه برنامه‌ها و برنامه‌های میان افزار خود را اضافه خواهیم کرد.
url:یک ادرس یابس کلی در پروژه هست


secret key:نگهدارنده کد رمز نگاری اطلاعات پروژه

MVT:معماری جنگو
MVT:model , view , template
MVT:یک الگوی طراحی نرم‌افزار است

model:مدل قرار است به‌عنوان رابط داده‌های شما عمل کند. این مسئول حفظ داده‌ها است. این یک ساختار داده منطقی در پشت کل برنامه است و توسط یک پایگاه داده نشان داده میشود
model:مدیریت دیتابیس

template:الگو رابط کاربری است - چیزی که هنگام ارائه یک وب‌سایت در مرورگر خود مشاهده می‌کنید

view:مانند چارچوبی است که ارتباط بین کاربر و پایگاه‌داده و انتقال صفحات را کنترل می‌کند و بسیاری از قسمت‌های دردناک مدیریت یک وب‌سایت پیچیده را خودکار می‌کند.
view:مدیریت بست دادن ادرس هابه صفحات


# HTTP method

http: یک پروتوکل ارتباطی امن نیست

http: HTTP یک زبان مکالمه یا مجموعه‌ای از قوانین است که بر اساس آن مرورگر شما (کلاینت) و سروری که جنگو روی آن اجرا می‌شود (سرور) با هم صحبت می‌کنند.

post:غلب برای ایجاد ساخت منابع جدید استفاده می‌شود. به طور خاص‌، برای ایجاد منابع تابع استفاده می‌شود

get:برای خواندن (یا بازیابی) نمایش یک منبع استفاده می‌شود
get:میشه اطلاعات رو هم باهاش جابه جا کرد که یکسری محدودیتها دارد

put/patch:برای تغییر و یا تصحیح اطلاعات مورد استفاده قرار می‌گیرند

delet:منحصرا از این متد برای حذف یک داده در پایگاه داده‌ها مورد استفاده قرار می‌گیرد.

request.method → نوع درخواست (GET, POST, etc)
request.GET → تمام پارامترهای ارسال‌شده با متد GET
request.POST → اگر فرم با method=“post” ارسال بشه، داده‌ها اینجا هستند

#status codes
< 1xx >
informational responses     اطلاع رسانی
پاسخ ابتدایی به یم درخواست

< 2xx >
successful responses        موفقیت
درخواست شما باموفقیت پردازش شده

< 3xx >
Redirection responses       انتقال
این کدها نشان‌دهنده نیاز به انتقال یا ارجاع به یک URL دیگر هستند

< 4xx >
client error responses      خطای کلاینت
این کدها نشان‌دهنده خطاهایی هستند که به دلیل درخواست نادرست کلاینت بوجود آمده‌اند.
درخواست نادرست کاربر ایجاد میکنه

< 5xx >

server error responses      خطای سرور
این کدها نشان‌دهنده مشکلاتی هستند که در سرور اتفاق افتاده و باعث شده‌اند که درخواست به درستی پردازش نشود.
مشکلات سمت سرور را نمایش میدهد





view

HttpResponse = برای برگرداندن رشته یا اج تی ام ال به کاربر استفاده میشه
JsonResponse = ساختار جیسون برمیگردونه

Url
Uniform Resource Locators
ادرس اینترنتی:توضیح


href
یک ویژگی برای تگ های تایین شده که ادرس یو ار ال میگیرند:توضیح
مثال: <a href="https://example.com/about/">درباره ما</a>


Protocol
پروتوکل های استاندارد وب http , https


host  ==   hostname + port
ادرسی میباشد که به جای ایپی نوشته میشود و برایشناسایی وب سایت از ان استفاده می‌شود


دامنه ها و کاربردان ها

com:دامنه های تجاری که اکثر دامنه های موجود را شامل میشود

org:دامنه های سازمانی، که میتوان هر نوع سازمانی را شامل شود.

gov:آژانس دولتی ، معمولا فدرال ولی میتواند دولتی هم باشد.

edu:دامنه برای موسسه های آموزشی

net:که بیشتر ارائه دهنده های سرویس های اینترنی استفاده می کنند. 

ir:iran

fr:france


پروتوکل ها و پورت های ان ها

80	    HTTP	سایت‌های بدون SSL
443	    HTTPS	سایت‌های امن
21	    FTP	    انتقال فایل
22	    SSH	    دسترسی امن به سرور
25      / 587	SMTP	ارسال ایمیل
3306	MySQL	پایگاه داده
5432	PostgreSQL	پایگاه داده

پورت = شماره‌ای برای شناسایی سرویس خاص روی یک سیستم

Hash:متصل کننده المان ها به یکدیگیر و به نوعی برای جا به جایی بین المان های موجود در یک صفحه مورد استفاده قرار میگیرد

ROOT_URLCONF:مشخص میکنم که فایل اصلی تعریف مسیرها در پروژه کجاست

STATIC_URL:ادرس فایل که در ان فایل های قالب قرار دارد را دریافت میکنم

MEDIA_URL:ادرس پایه برای فایل های اپلودی از طریق کاربر را مشخص میکند



python manage.py:میتوان دسترسی داشت manage.pyبه دستورات دیگر 


بارگیری قالب سایت ها
دستور زیر به همراه ادرس سایت قالب سایت مورد نظر را بارگیری میکند
shift + کلیک راست
./wget.exe -r 




Alt + shift + f == منظم کردن کد
Alt + kilic = انتخاب کردن چند تا خط و همزمان نوشتن در همه خطها
ctrl +  h = سرچ کلمات

#پاک کردن کش
#ctrl + shift + R

برابر با STATIC و MEDIA محل اضافه کردن آدرس های url در طول پروژه در بخش ==== urlpatterns



##template tag                  {%  %}
عملکردی را مثل یک تابع برای کاری انجام می‌دهد

Display Logic. E.g., {% if %}...{% endif %}
Loop Control. E.g., {% for x in y %}...{% endfor %}
Block Declaration. E.g., {% block content %}...{% endblock %}
Content Import. E.g., {% include "header.html" %}
Inheritance. E.g., {% extends "base.html" %}



##template variable             {{  }}
این متغیرها در زمان اجرای صفحات می‌توانند به آن ارجاع داده شده و المان‌هایی را به نمایش بگذارند که به‌صورت داینامیک در صفحه قرار می‌گیرند.
Simple Variables. E.g., {{ title }}
Object Attributes. E.g., {{ page.title }}
Dictionary Lookups. E.g., {{ dict.key }}
List Indexes. E.g., {{ list_items.0 }}
Method Calls. E.g., {{ var.upper }}, {{ mydict.pop }}



## filter   |
شما با استفاده از | فیلتر را به یک متغیر اعمال می‌کنید
Change Case. E.g., {{ name|title }} or {{ units|lower }}
Truncation. E.g., {{ post_content|truncatewords:50 }}
Date Formatting. E.g., {{ order_date|date:"D M Y" }}
List Slicing. E.g., {{ list_items|slice:":3" }}
Default Values. E.g., {{ item_total|default:"nil" }}


#fillters
truncatechars:100    ==  برای نمایش 100 کاراکتر اول
truncateword:100 == برای نمایش 100 کلمه اول
date:"d/m/Y" == برای فرمت‌دهی تاریخ‌ها هنگام نمایش در قالب‌ها استفاده می‌شود


static:یعنی ثابت و بدون تغییر در زمان اجرا.
dynamic:یعنی محتوای متغیر که هنگام درخواست کاربر ساخته می‌شود.



{% load static %} : در ابتدای صفحه برای بارگیری عملکرد های کار با استاتیک هاست


jinga: یه موتور قالی هست که در جنگو برای استفاده کد پایتون در کد اچ تی ام ال استفاده میشه و قالب‌ها را به‌صورت دینامیک تولید کنیم

jinga: یک موتورو کار با صفحات وب است

Extends:ارث بری یک صفحه در دیگر صفحات
Extends:برای ارث‌بری کامل از یک قالب مادر استفاده می‌شود

include:برای درج یک بخش کوچک یا تکراری از قالب در صفحه دیگر است

#api
راهی‌ست برای ارتباط بین نرم‌افزارها. با استفاده از آن، یک برنامه می‌تواند از قابلیت‌های برنامه‌ی دیگر بدون دانستن جزئیات داخلی‌اش استفاده کند.

#crud
#چهار عمل اصلی api
create
retrieve
update
delet



#Makemigrations: کارهایی که میخوام انجام بشه و تاالان انجام نشده رو بهش میدم
#migrate: اون کارهای تایین شده رو نجام بده

پوشه Migrations : محل نگهداری تغییرات Model مربوط به هر اپلیکیشن است.
migration: نهایی کردن تغییرات مبتنی بر کوعری های ایجاد شده در پوشه و ایجاد دیتابیس
دستور makemigrations : ساخت اولیه و آماده‌سازی query های لازم برای ایجاد تغییرات دیتا بیس

sqlite: جنگو به صورت پیش فرض از این دیتابیس استفاده میکنه

orm: فقط یک روش  برای ساخت و دستکاری پایگاه داده با محوریت زبان پایتون است که بدون نیاز به تغییر نوع کوعری ها با یک زبان با پایگاه داده ها متفاوت ارتباط برقرار نمایید.
orm:یک پل ارتباطیه


python manage.py makemigrations
python manage.py migrate
python manage.py createsuperuser 
دستور createsuperuser : برای ساخت یک یوزر اولیه با دسترسی Super

# fild option
#models.DateTimeField(auto_now_add=True)
#auto_now_add = برای زمانی که آبجکت ایجاد می‌شود استفاده می‌شود

#models.DateTimeField(auto_now=True)
#auto_now = رای ذخیره‌سازی زمان ایجاد تغییرات

function table models __str__ =  یک فرم نمایشی منطقی متنی را به جای آدرس و شماره ابجکت نمایش مید

# shell django
python manage.py shell:بازکردن شل جنگو
p = Post()
p.title = 'test title'
p.content = 'this is a content'
p.status = True
p.save()

p.delete()
Post.objects.all()
Post.objects.all().delete()
Post.objects.filter(status=True)
Post.objects.filter(status=False)
Post.objects.all()[0]
Post.objects.get(id=1) # get the object which has id equal to 1

import os
os.system('cls')

#create
from mysite.models import Post
p = Post()
p.title = “title”
p.content = “contents of the post”
p.save()


#update
p = Post.objects.get(id=1)
p.title = “change”
p.save()


#object.all()
Post.objects.all() # all objects in post table
Post.objects.filter(status=1) # all objects which has status equal to 1 or True
Post.objects.get(id=1) # get the object which has id equal to 1


#delet
p = Post.objects.all()
p = Post.objects.get(id=1)
p = Post.objects.get(id=pid)
p = post.objects.filter(status=True)
p.delete()

from django.core.paginator import Paginator
has_next = گرفتن تشخیص وجود پست بعدی
has_previous = برای تشخیص وجود پست قبلی
end_index = برای گرفتن ایندکس اخر
page_range = یک دنباله ازاعداد صفحات را به شما باز می گرداند
start_index = برای گرفتن ایندکس صفحه اول استفاده می‌شود

## django admin  customization

نکته: برای استفاده از url فارسی در جنگو باید در مدل‌هاتون آپشن allow_unicode  در فیلد SlugField را برابر با True قرار دهید تا قابلیت پردازش پیدا کند در غیر این صورت تنها انگلیسی و کاراکترهای مبتنی بر آن را خواهد شناخت.


{% url ‘blog:single’ pid=post.id  %}
/blog/post-{{item.id}}
path('<int:pid>' ,blog_single,name='single')
{% url ‘blog:single’ parameter1=object1   parameter2=object2   … %}


M نمایان‌گر ماه کوتاه‌شده است (مثلاً Jan، Feb، Mar و …).
D نمایان‌گر روز هفته به‌صورت کوتاه‌شده است (Fri، Mon، Tue و …).

در فیلتر date، قالب "M D" به‌صورت زیر نمایش داده می‌شود:

ImageField = media


include =  برای وارد کردن بخشی از یک قالب دیگر در صفحه‌ای که در حال اجراست استفاده می‌شود.

extends = برای ارث‌بری از یک قالب اصلی (پدر) استفاده می‌شود.



وجود فایل __init__ در پوشه template tag ضروری است

 نامگذاری پوشه برای استفاده ارTemplate Tag: templatetags

شمارش تعداد آبجکت‌های یک query: count

inclusion_tag: صفحاتی را با اطلاعاتی از طریق یک دیکشنری در جایگاه خاصی render کنند.

تغییر نام template tag: name


Messages framework:چارچوب پیام‌ها به شما امکان می‌دهد تا پیام‌ها را به طور موقت در یک درخواست کاربر 
ذخیره کرده و پس از پردازش در بک‌اند عملکرد نسبت به کار انجام شده پیغام منحصربه‌فرد را تخصیص دهید 
که این پیغام‌ها می‌توانند در پاسخ به عملکرد درست یا غلط و خطاهای احتمالی 


در صورتی که تابع ورودی view متغیری نیاز داشته باشد و ورودی آن از url  تامین نشود چه اتفاقی رخ می‌دهد؟
با خطای 404 رو به رو می‌شوید.



templatetags {% func as obj %} = مقداری را برمی‌گرداند و می‌خواهی آن مقدار را در یک متغیر ذخیره کنی تا بعداً در همان قالب از آن استفاده کنی، باید از ساختار {% ... as ... %} استفاده کنی.


#module کتابخونه

#paginator == این کلاس کمک می‌کنه لیست بلند داده‌ها (مثلاً صدها یا هزاران رکورد) رو به چند صفحه تقسیم کنی تا کاربر به جای دیدن همه اون‌ها، هر بار فقط مثلاً ۱۰ یا ۲۰ آیتم ببینه.
posts.has_next = ایا پیج بعدی وجوددارد 
posts.next_page_number = عدد مربوط به پیج بعدی
posts.has_previous = ایا پیج قبلی وجوددارد
posts.previous_page_number = عدد مربوط به پیج قبلی

paginator.count → تعداد کل آیتم‌ها
paginator.num_pages → تعداد کل صفحات
paginator.page_range → بازه شماره‌های صفحات (مثلاً [1, 2, 3, 4, 5])
paginator.page(number) → شیء صفحه خاص
paginator.get_page(number) → امن‌تر از page() (خودکار خطاها رو هندل می‌کنه)



CSRF ==                 ?????????????????????????????????????????
<input type="hidden" name="csrfmiddlewaretoken" value="0J0acXoIZQkuaE1dnoNFulVoQdm3DAOMnqpQzNJCUhgoBtLv4AWXvl1GylUZ9IE4">
						

#مشکلات

1.مشکل pop up message practice 3 مشکل کتابخونه
2.مشکل کتابخونه Django-summernote
3.مشکل راست به چپ در نمایش تایتل و کانتنت درپوست ها روی صفحه اصلی 
4.disqus = سیستم کامنتینگ که نیاز به اینترنت ملی یا حتی فیلتر 
5.درست کردم سرچت تمام صفحات 
6.درست کردن نظم صفحه اچتی ام ال در صفحه سینگل
7.درست کردن کد html login == به خاطر نت ملی
8.درست کردن content در ادمین


blank=True: این پارامتر به اعتبارسنجی فرم (form validation) اجازه می‌دهد که این فیلد خالی باشد
null=True: این پارامتر به دیتابیس اجازه می‌دهد که مقدار NULL را برای این ستون ذخیره کند


pop-up = یعنی یک پنجره کوچک و شناور که روی صفحه ظاهر می‌شود و کاربر را آگاه می‌کند.
pop up notification = این دقیقاً همان پاپ اپ است،‌ فقط نوع خاصی از آن که مخصوص نمایش پیام‌ها / نوتیفیکیشن‌ها است.


# module django
1.humanize
1.Django extensions  === توانایی تولید مدل ندارد ولی توانایی تولید شماتیک مدل را دارد


Django Sitemap Framework

ین فریم‌ورک جنگو برای ایجاد نقشه سایت به صورت فایل xml  استفاده می‌شود، نقشه سایت یک فایل  
XML در وب‌سایت شماست که به نمایه‌های موتورهای جستجو می‌گوید که صفحات شما
 چگونه در سرور قرار گرفته و یا تغییر می‌کند و صفحات خاصی در مقایسه با سایر صفحات سایت شم
ا چقدر "مهم" هستند. این اطلاعات به موتورهای جستجو کمک می‌کند تا سایت شما را فهرست‌بندی کنند. 


#Django Robots
این ماژول در اصل برنامه‌ای است که برای ساخت فایل robots سایت جنگو استفاده می‌شود که تکمیل‌کننده sitemap  سایت است

بیشترین نقش robots.txt: ربات‌های موتورهای جستو گر


///
Django debugger را می‌توان در production  استفاده کرد؟
خیر ، صرفا استفاده development  دارد
///


# Django-summernote
این ماژول برنامه‌ای است که به شما اجازه می‌دهد تا یک ویرایشگر برای اطلاعات خود در سایت خود جای‌گذاری کنید


#  Django Taggit 
این ماژول برنامه ای است که به شما اجازه می دهد تا تگ های مورد نظر را به صفحات وب خود اضافه کنید.

#  Django-simple-captcha
این ماژول برنامه ای است که به شما اجازه می دهد تا امنیت سایت خود را هنگام ورود اطلاعات در فرم‌ها در سایت تامین کنید


# احراز هویت کاربر
request = در ویو تشخیص ورود کاربر از طریق ریکوعست بررسی میشود

Authentication = ماژول کنترل ارتباطات احراز هویت در دیجنگو

Is_authenticated = برای تشخیص ورود کاربر به تمپلیت

Authenticate = تابع مربوط به اهراز هویت در ویو ها پس از دریافت اسم و پسورد


Login_required = دکوراتور مربوط به استعلام ورود کاربر قبل از اجرای  ویو  در ماژول  Auth